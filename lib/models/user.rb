class User < Utility
  # This class doesn't have any specific functionality as of now, but
  # we can use the attribute accessors defined as below and update this
  # class depending the upcoming requirements

  # attr_accessor :_id, :url, :external_id, :name, :alias, :created_at, :active, :verified,
  #               :shared, :locale, :timezone, :last_login_at, :email, :phone, :signature,
  #               :organization_id, :tags, :suspended, :role
end
