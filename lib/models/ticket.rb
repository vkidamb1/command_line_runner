class Ticket < Utility
  # This class doesn't have any specific functionality as of now, but
  # we can use the attribute accessors defined as below and update this
  # class depending the upcoming requirements

  # attr_accessor :_id, :url, :external_id, :name, :created_at, :type, :subject, :description,
  #               :priority, :status, :submitter_id, :assignee_id, :organization_id,
  #               :has_incidents, :due_at, :via, :tags
end
